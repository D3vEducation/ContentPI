type App {
  id: UUID!
  userId: UUID!
  appName: String!
  icon: String!
  description: String!
  createdAt: Datetime!
  updatedAt: Datetime!
  models: [Model!]
}

type Query {
  getApps: [App!]
  getApp(appName: String!): App!
}

type Mutation {
  createApp(input: CreateAppInput): App!
}

input CreateAppInput {
  appName: String!
  icon: String!
  description: String!
  userId: UUID!
}
